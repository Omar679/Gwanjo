import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CC = () => {
  return (
    <View>
      <Text>CaC</Text>
    </View>
  );
};

export default CC;

const styles = StyleSheet.create({});
